import { dark } from './dark/index';
import { light } from './light/index';
import { samurai } from './samurai/index';

export const chakraTheme: Record<string, Record<string, any>> = {
  light,
  dark,
  samurai,
};
